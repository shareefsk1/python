def show_numbers(number):
    #Function
    for i in range(number+1) :
        a = i % 2
        if a == 0 :
            print(str(i) + " EVEN")
        else :
           print(str(i) + " ODD") 

number = int(input())
# Call the show_numbers function
show_numbers(number)
