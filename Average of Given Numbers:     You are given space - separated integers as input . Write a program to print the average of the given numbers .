a = input().split()#converting the string into a list
length = len(a)

#converting list items into integer and adding all integers
sum_of_integers = 0
for i in a :
    sum_of_integers += int(i)

#avarage of items
avarage = sum_of_integers / length
print(round(avarage,2))
