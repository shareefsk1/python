first_input_range = int(input())
second_input_range = int(input())

for i in range(first_input_range,second_input_range+1) :
    carrying_variable = 0
    for j in range(2,i+1) :
        if i % j == 0 :
            carrying_variable = carrying_variable + 1
    if carrying_variable >= 2 :
        print(i)
